services:
  backend:
    build: .
    working_dir: /app/todo  
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: test
      DEBUG: "True"
      PYTHONPATH: /app/todo
    stdin_open: true
    tty: true